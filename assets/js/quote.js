const form=document.querySelector('#quote-form');const service=document.querySelector('#service');const cabinets=document.querySelector('#cabinet-fields');const photos=document.querySelector('#photos');const photoStatus=document.querySelector('#photo-status');const message=document.querySelector('#form-message');service?.addEventListener('change',()=>{cabinets.hidden=service.value!=='Cabinet Refinishing'});photos?.addEventListener('change',()=>{const count=photos.files.length;if(count>10){photos.value='';photoStatus.textContent='Please choose no more than 10 photos.';return}photoStatus.textContent=count?`${count} photo${count===1?'':'s'} selected`:'Choose photos from your phone or computer'});form?.addEventListener('submit',event=>{event.preventDefault();message.textContent='The form layout is ready. Secure submission delivery will be enabled before launch.';message.scrollIntoView({behavior:'smooth',block:'center'})});