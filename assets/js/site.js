async function loadJSON(path){try{const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error();return await r.json()}catch{return null}}
function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
(async()=>{const projects=await loadJSON('content/projects.json');const pg=document.querySelector('#project-grid');if(pg&&projects?.length){pg.className='project-grid populated';pg.innerHTML=projects.filter(p=>p.published!==false).map(p=>`<article class="project-card"><img src="${esc(p.cover)}" alt="${esc(p.alt||p.title)}" loading="lazy"><div><small>${esc(p.service)} · ${esc(p.location)}</small><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p></div></article>`).join('')}
const reviews=await loadJSON('content/reviews.json');const rg=document.querySelector('#review-grid');if(rg&&reviews?.length){rg.innerHTML=reviews.filter(r=>r.featured!==false).slice(0,4).map(r=>`<blockquote>“${esc(r.text)}”<footer>— ${esc(r.name)} · Google Review</footer></blockquote>`).join('')}})();